{
  "name": "threejs-web-components",
  "packageManager": "yarn@4.9.2",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspaces foreach --from packages/examples -Rpi run dev",
    "build": "yarn workspaces foreach --from packages/examples -Rt run build"
  }
}
